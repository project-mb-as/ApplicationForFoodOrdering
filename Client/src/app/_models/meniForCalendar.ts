import { Data } from '@angular/router';
import { Moment } from 'moment';
import * as moment from 'moment';

export class MeniForCalendar {
    meniId: number;
    datum: Data;
    canOrder: boolean;
}
